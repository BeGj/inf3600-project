# Python
**/.venv/
**/__pycache__/
.env
outputs/

# Node
**/node_modules/
frontend/dist/

# Model weights (large; ship via volume/release). Keep the manifest + README.
backend/models/**/*.safetensors
backend/models/**/*.bin

# Notebook training outputs (peft checkpoints + sample renders are regenerable).
notebooks/lora-satellite-inpaint/
notebooks/inpaint_result.png

# Datasets (keep structure, ignore contents)
datasets/*
!datasets/houses/
!datasets/trees/
!datasets/houses/.gitkeep
!datasets/trees/.gitkeep