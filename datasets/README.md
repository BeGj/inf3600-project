# Datasets

Generated local training datasets live here.

The current prepare scripts write to:

- `houses/` for `notebooks/prepare/prepare_houses.py`
- `trees/` for `notebooks/prepare/prepare_trees.py`

The dataset contents are ignored by Git. Keep one subfolder per dataset and add more subfolders here when new dataset preparation scripts are added.
